// import { SWITCH_THEME } from "../actions/action-types";
// import { lightModeTheme, darkModeTheme } from '../../constants/index';

// const initialState = darkModeTheme;

// function currentThemeReducer(state = initialState, action) {
//    if (action.type === SWITCH_THEME) {
//       return action.payload
//    }
//    return state;
// };

// export default currentThemeReducer;
